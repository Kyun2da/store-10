import React from 'react';
import styled from 'styled-components';

const index = () => {
  return <Header>이것은 헤더헤더</Header>;
};

export default index;

const Header = styled.header`
  padding: 1rem;
  font-size: 2rem;
`;
